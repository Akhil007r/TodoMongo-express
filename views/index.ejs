<%-include('header')-%>
    <div class="box" id="heading">
        <h1>
            <%= listTitle %>
        </h1>

    </div>
    <div class="box">
        <!-- <% for(var i=0 ; i<newListItem.length ; i++){ %>
            <div class="item">
                <input type="checkbox" />
                <p>
                    <%= newListItem[i] %>
                </p>
            </div>

            <% } %> -->
        <!-- or use for each method -->
        <% newListItem.forEach(function(item){ %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%=item._id %>" onchange="this.form.submit()" />
                    <p>
                        <%=item.name %>
                    </p>
                    <input name="listName" value="<%= listTitle %>" type="hidden">
                </div>
            </form>
            <% }) %>
                <form class="item" action="/" method="post">
                    <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
                    <button type="submit" name="lists" value="<%=listTitle %>">+</button>
                </form>
    </div>

    <%-include('footer');-%>


        <!-- <% if(listTitle === "Saturday"||listTitle === "Sunday") {%>
    <h1 style="color: blueviolet;">it's a <%=listTitle%> !</h1>
   <% }else{ %>
    <h1 style="color: blue;">it's a <%=listTitle%> !</h1>

    <%}%> -->